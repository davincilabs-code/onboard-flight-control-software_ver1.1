#include "face_context.h"

void pcs_flight_management_point_to_point_flight_mode_step(FaceContext* ctx)
{
    // Waypoint definition and velocity command are handled in Path Planning & Tracking.
    (void)ctx;
}
